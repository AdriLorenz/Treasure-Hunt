const API_URL = 'http://localhost:5000/api';

const AWARDS = `${API_URL}/awards`;
const CATEGORIES = `${API_URL}/categories`;
const PLACES = `${API_URL}/places`;
const TOURS = `${API_URL}/tours`;
const TYPES = `${API_URL}/types`;
const USERS = `${API_URL}/users`;

export {
  API_URL,
  AWARDS,
  CATEGORIES,
  PLACES,
  TOURS,
  TYPES,
  USERS
};
